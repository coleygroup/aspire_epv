# ORD Message Prototype Editor

1. Visualization for the tree structure of ORD messages (defined as `protobuf`).
2. Editor for constructing message prototypes by presetting tree structure and fields.
